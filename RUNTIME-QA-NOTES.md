# File 01 Runtime QA Notes — 1.2.0

The GitHub harness installs disposable WordPress/MySQL, activates the exact branch source, runs File 01 runtime assertions, exercises concurrent idempotency and a disposable destructive-purge smoke, and feeds deterministic packaging only after source/runtime jobs succeed.

Version 1.2.0 additionally exercises exact schema/index integrity, structured File 00 actor/object/purpose claims, fail-closed ungated feature activation followed by verified activation evidence, File 20/21 reversible reconciliation receipts, audit-chain integrity, privacy lifecycle and upgrade compensation.

WP-CLI `eval-file` scripts omit `declare(strict_types=1)` because WP-CLI evaluates file contents inside its runtime wrapper. External Hostinger/browser/accessibility/load/backup/rollback/Founder gates remain separate.
