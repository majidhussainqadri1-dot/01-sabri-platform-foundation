# File 01 Runtime QA Notes — 2.0.0

The GitHub harness installs disposable WordPress/MySQL, activates the exact branch source, runs File 01 runtime assertions, exercises Future Foundation controls, concurrent idempotency and a disposable destructive-purge smoke, and feeds deterministic packaging only after source/runtime jobs succeed.

The 2.0.0 suite inherits the earlier 1.2.0 schema/index, File 00 actor/object/purpose claim, feature-activation, File 20/21 reversible reconciliation, audit-chain, privacy and upgrade-compensation regressions, then adds the later registry, release-state, event, resilience and release-handoff corrections.

WP-CLI `eval-file` scripts omit `declare(strict_types=1)` because WP-CLI evaluates file contents inside its runtime wrapper. External Hostinger/browser/accessibility/load/backup/rollback/Founder gates remain separate and cannot be inferred from this CI harness.
